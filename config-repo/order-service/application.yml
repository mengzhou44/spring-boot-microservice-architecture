# Optional overrides for order-service (config server)
buildNumber: "2.1.0"

# Uncomment and adjust to override when using config server:
# server:
#   port: 8082
# eureka:
#   client:
#     service-url:
#       defaultZone: http://localhost:8761/eureka/
